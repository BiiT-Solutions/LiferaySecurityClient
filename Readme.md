# Liferay Security Client
Authentication and authorization services based on Liferay.

# Usage

This is a Jar library that can be imported in any maven project as follows:

```
<dependency>
    <groupId>com.biit-solutions.liferay</groupId>
    <artifactId>liferay-authentication-client</artifactId>
</dependency>
```